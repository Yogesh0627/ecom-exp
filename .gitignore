# Secrets — never commit
.env
.env.*
!.env.example
!.env.local.example
*.pem
*.p12

# Java / Maven
target/
!.mvn/wrapper/maven-wrapper.jar
*.class
*.jar
*.war

# Node / Next.js
node_modules/
.next/
out/
dist/
build/
.pnp.*
.yarn/*

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*
logs/

# Local object storage (dev uploads; prod uses R2).
# Anchored to the exact folder — a bare "storage/" would also ignore the SOURCE package
# server/src/main/java/com/ecoexpress/common/storage (gitignore matches the name anywhere).
/server/storage/
